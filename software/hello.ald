# This is a simple hello world program

# MAIN

        MOV CX 0x00
LOOP:
        PRINTCHAR [TEXT+CX]B
        ADD CX CX 0x01
        JLT CX [TEXT_LENGTH]B LOOP
        SHUTDOWN

# DATA

TEXT:
        .DAT 'Hello world'

TEXT_LENGTH:
        .DAT 0x0B
